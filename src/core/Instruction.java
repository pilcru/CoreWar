package core;

/**
Représente le type d'une operation. La définition de ces types est dans la classe Memoire
 */
public enum Instruction {
	MOV, ADD, SUB, JMP, JMZ, JMN, CMP, SLT, DJN, SPL, DAT;
}
